  {{this.id}}. {{this.burger_name}}

    <button type = "button" class="devour-burger btn btn-success" data-id="{{id}}" data-newdevour="{{devoured}}">
        {{#if devoured}}EAT-Da-Burger!{{else}}THROW UP BURGER!{{/if}}
    </button>

  

    {{!-- {{this.id}}. {{this.burger_name}} {{this.devoured}} --}}



    